overwrite: true
schema: "schema.graphql"
generates:
  src/codegen/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
  graphql.schema.json:
    plugins:
      - "introspection"
