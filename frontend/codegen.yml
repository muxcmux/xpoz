overwrite: true
schema: "http://localhost:1234/api"
generates:
  src/codegen/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
  graphql.schema.json:
    plugins:
      - "introspection"
